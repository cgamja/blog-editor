## ADDED Requirements

### Requirement: 조립한 에디터에 정렬 단축키가 있다

`blogEditorExtensions()`는 SHALL editor-core의 `AlignKeys` 확장을 포함한다.

#### Scenario: 조립한 목록에 alignKeys가 있다

- **WHEN** `blogEditorExtensions()`의 확장 이름을 본다
- **THEN** `alignKeys`가 있다
