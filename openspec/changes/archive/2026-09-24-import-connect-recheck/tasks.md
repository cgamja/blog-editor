# Tasks — import-connect-recheck

## 1. 테스트

- [x] 1.1 api `settings.test.ts` · `import-preview.test.ts` · `file-settings-store.test.ts`에 413 · 틀린 설정 파일 시나리오 → verify: 처음부터 초록(동작 고정)

## 2. Converge

- [x] 2.1 `pnpm verify` 초록 · `openspec validate --all --strict`
